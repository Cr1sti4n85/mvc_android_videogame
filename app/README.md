
¿Tuviste que cambiar la lógica de los botones para agregar este dato o solo el flujo de información?

Para el lanzamiento se tuvo que actualizar el modelo Videogame, se actualizó la lista del repositorio
Se agregó una línea en el método setupListener para mostrar el año,
se transformó a String ya que el dato viene como numero entero